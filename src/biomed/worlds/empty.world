<?xml version="1.0" ?>
<sdf version="1.6">
  <world name="empty_world">
    <!-- Zemin modeli -->
    <include>
      <uri>model://ground_plane</uri>
    </include>

    <!-- Güneş / ışıklandırma -->
    <include>
      <uri>model://sun</uri>
    </include>

    <!-- Yerçekimi ve fizik ayarları (isteğe bağlı) -->
    <physics name="default_physics" default="true" type="ode">
      <gravity>0 0 -9.81</gravity>
    </physics>
  </world>
</sdf>
